import styles from "./Main.module.scss";import { Circles } from "../components/circles/Circles.tsx";import { Apparate } from "../components/apparate/Apparate.tsx";import { ApparateContextProvider, useApparateContext } from "../components/apparate";import Weight from "../components/weight/Weight.tsx";import { ThermoSensor } from "../components/thermoSensor/ThermoSensor.tsx";import Header from "../components/header/Header.tsx";import "../i18n/i18n.tsx";import { Instruction } from "../components/instruction/Instruction.tsx";const MainContent = () => {    const { currentToggle } = useApparateContext();    return (        <div className={styles.dynamicSlot}>            {currentToggle === 0 && <Weight />}            {currentToggle === 1 && <ThermoSensor />}            {currentToggle === 2 && (                <div style={{ marginTop: '30px', textAlign: 'center' }}>                    <img                        src={`${import.meta.env.BASE_URL}meme.png`}                        alt="Plans for Monday"                        style={{ maxWidth: '400px', borderRadius: '12px', boxShadow: '0 4px 15px rgba(0,0,0,0.2)' }}                    />                </div>            )}            {currentToggle === 3 && <Circles />}        </div>    );};export const Main = () => {    return (        <div>            <Header />            <Instruction />            <ApparateContextProvider>                <Apparate />                <MainContent />            </ApparateContextProvider>        </div>    );};